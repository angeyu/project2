console.log("#loveyoself")

// todayDate
const date = moment().format("MMMM Do YYYY");
$("#todayDate").text(date);

// toggle button boolean attribute change
const queryUrlGET = 
$.ajax({
    url: queryUrlGET,
    method: "GET"
}).then(function(tableData) {
    for (const index = 0; index < tableData.length; index++) {
        
        
    }
})

// facts or affirmation api call

// "i dont do this" null value



// avatar image state change (size -- for now, 3 state changes: small, medium, large)